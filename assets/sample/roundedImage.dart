Container(
      width: size,
      height: size,
      decoration: BoxDecoration(
        color: bgColor,
        image: image,
        borderRadius: BorderRadius.all(
          Radius.circular(size / 2),
        ),
        border: Border.all(
          color: Colors.black26,
          width: 2.0,
        ),
      ),
    )